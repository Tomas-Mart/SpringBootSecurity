# Настройки базы данных (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/rest?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Настройки HikariCP
spring.datasource.hikari.pool-name=SecurityDemoPool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Настройки JPA/Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Оптимизации
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# Настройки сервера
server.port=8080
server.address=0.0.0.0
server.tomcat.connection-timeout=30000
server.servlet.context-path=/
server.servlet.session.timeout=1800
server.servlet.session.cookie.http-only=true
server.tomcat.keep-alive-timeout=20000
server.tomcat.max-keep-alive-requests=100
spring.main.web-application-type=servlet
spring.main.lazy-initialization=false

# Thymeleaf configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# Логирование
logging.level.root=DEBUG
logging.level.ru.kata=DEBUG
logging.level.org.thymeleaf=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE
logging.level.org.springframework=INFO
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.ru.kata.spring.boot_security.demo=DEBUG

# Настройки CORS
cors.exposed-headers=Authorization
cors.allowed-origins=http://localhost:8080
cors.allowed-methods=GET, POST, PUT, DELETE, OPTIONS
cors.allowed-headers=Authorization, Content-Type, X-XSRF-TOKEN

# Настройки CSRF
spring.security.csrf.enabled=false

# Настройки DevTools
spring.devtools.restart.enabled=true
spring.devtools.restart.exclude=static/**,templates/**

# Spring Security configuration
spring.security.user.name=admin
spring.security.user.password=admin
spring.security.user.roles=ADMIN

# Настройки статических ресурсов
spring.mvc.static-path-pattern=/resources/**

# Сообщения валидации
user.username.notblank=Имя пользователя не может быть пустым
user.username.size=Имя пользователя должно быть от {min} до {max} символов
user.email.notblank=Email не может быть пустым
user.email.size=Email должен быть от {min} до {max} символов
user.password.notblank=Пароль не может быть пустым
user.password.size=Пароль должен содержать минимум {min} символов
user.firstName.notblank=Имя не может быть пустым
user.firstName.size=Имя должно быть от {min} до {max} символов
user.lastName.notblank=Фамилия не может быть пустой
user.lastName.size=Фамилия должна быть от {min} до {max} символов
user.age.notnull=Возраст обязателен
user.age.min=Возраст не может быть отрицательным
user.roles.notempty=Необходимо выбрать хотя бы одну роль

# Для детальной диагностики
logging.level.com.zaxxer.hikari=TRACE
logging.level.com.zaxxer.hikari.pool=DEBUG
